# birdify

Command-line tools for Birdify architecture maps, activity records, constraint review and project mode.

```sh
npx --yes @borg0ai/birdify --help
```

Node.js 22 or newer is required. The skill and this CLI install separately.
